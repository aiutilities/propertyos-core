# Plugin Engine

The Plugin Engine manages plugin lifecycle, validation, installation, activation, configuration, updates, rollback and removal.

It is the core module responsible for making PropertyOS extensible without modifying core code.
